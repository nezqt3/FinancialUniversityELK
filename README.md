# MAX Hackathon stack

Проект состоит из двух независимых частей:

- `backend/textbot` — нативный бот на `@maxhub/max-bot-api`, который отправляет кнопку `startapp`
- `miniapp` — React-приложение на MAX UI, умеет работать с [MAX Bridge](https://dev.max.ru/docs/webapps/bridge) и стартовыми параметрами

## Настройка бота

1. Перейдите в `backend/textbot/.env` и укажите:
   - `BOT_TOKEN` — токен из MAX Business
   - `BOT_USERNAME` — публичный ник бота (используется в deep-link вида `https://max.ru/<bot>?startapp`)
2. Установите зависимости и запустите бот:

```bash
cd backend/textbot
npm install
node main.js
```

Команда `/start` отправляет inline-кнопку. Ссылка собирается динамически и содержит `startapp=<payload>`, где payload — base64url JSON с ID и именем пользователя. Это позволяет miniapp показать персональные данные сразу после открытия.

## Мини-приложение

1. Установите зависимости и запустите приложение в dev-режиме:

```bash
cd miniapp
npm install
npm start
```

2. Перед деплоем выполните `npm run build` и загрузите содержимое `miniapp/build` на HTTPS-хостинг (Vercel, GitHub Pages и т. д.). Эту ссылку нужно указать в MAX Business → **Чат-бот и мини-приложение** → **Настроить**, как описано в [документации](https://dev.max.ru/docs/webapps/introduction#Как%20открыть%20приложение%20по%20прямой%20ссылке).

### Локальная проверка start_param

MAX клиента нет в браузере, поэтому для локальной отладки можно пробросить payload вручную:

```bash
PAYLOAD=$(node -e "console.log(Buffer.from(JSON.stringify({firstName:'Demo',role:'abiturient'})).toString('base64').replace(/\+/g,'-').replace(/\//g,'_').replace(/=+$/,''))")
open \"http://localhost:3000/?WebAppStartParam=$PAYLOAD\"
```

Miniapp автоматически подключает `https://st.max.ru/js/max-web-app.js`, вызывает `WebApp.ready()` и пытается прочитать `window.WebApp.initDataUnsafe`. Если приложение запущено вне MAX, оно использует `WebAppStartParam` из query string и всё равно покажет полезную информацию.

## Как связка работает

1. Пользователь нажимает «🚀 Открыть мини-приложение» в чате бота.
2. MAX открывает ссылку `https://max.ru/<botName>?startapp=<payload>` внутри клиента и загружает miniapp по URL, который вы сохранили в MAX Business.
3. Клиент пробрасывает `payload` в `start_param` и в GET-параметр `WebAppStartParam`.
4. React-приложение декодирует payload, показывает имя/ID пользователя и сырые параметры, а также сигнализирует готовность через `WebApp.ready()`.

Таким образом miniapp мгновенно понимает, кто с ней работает, и может строить персональные сценарии (заявка, расписание, заявки в деканат и т. д.).
# MaxHackathon
Проект хакатон: мини-приложение и чат-бот.  

## Описание
Этот проект создан для хакатона и включает:
- Backend: чат-бот (`/backend/textbot`)
- Frontend: мини-приложение (`/miniapp`)
- Контейнеризацию через Docker

Проект позволяет запускать мини-приложение и взаимодействовать с чат-ботом.

## Структура директории
- /backend/textbot/ — код чат-бота
- /miniapp/ — мини-приложение (frontend)
- Dockerfile — контейнеризация проекта
- .gitignore — файлы и папки, игнорируемые Git

## Технологии
- JavaScript / HTML
- Node.js (если требуется)
- Docker

## Быстрый старт
1. Клонируйте репозиторий:
```bash
git clone https://github.com/nezqt3/MaxHackathon.git
cd MaxHackathon
```

2. Соберите Docker-образ
```bash
docker build -t maxhackathon .
```

3. Запустите контейнер
```bash
docker run -p 3000:3000 maxhackathon
```

4. Запустите фронтенд мини-приложения
```bash
cd miniapp
npm install
npm start
```
